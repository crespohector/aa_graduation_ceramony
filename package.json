{
  "name": "virtual-graduation",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "homepage": "appgraduation.ml",
  "scripts": {
    "data": "node ./src/data/gradMapper.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx webpack -w --mode=development",
    "predeploy": "npm run build",
    "build": "webpack --watch --mode=production",
    "deploy": "npm run build && gh-pages -d dist",
    "pages": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "webpack": "^4.42.1",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
